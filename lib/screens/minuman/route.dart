import 'package:flutter/material.dart';

class MinumanRoute extends StatelessWidget {
  const MinumanRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Daftar Minuman'),
    );
  }
}
