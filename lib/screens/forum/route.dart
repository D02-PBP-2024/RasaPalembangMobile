import 'package:flutter/material.dart';

class ForumRoute extends StatelessWidget {
  const ForumRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Daftar Forum'),
    );
  }
}
