import 'package:flutter/material.dart';

class MakananRoute extends StatelessWidget {
  const MakananRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Daftar Makanan'),
    );
  }
}
